# Neovim Config

My neovim configuration for working on sveltekit projects

## Installation

Run this command to get the config up and running:

```sh
gh repo clone Isaachristian/neovim-config ~/.config/nvim
```
